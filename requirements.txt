# Core Dependencies
Flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.0

# Image Processing
Pillow==10.1.0

# PDF Processing - Only PyMuPDF is used (fastest, no external dependencies)
PyMuPDF==1.23.8